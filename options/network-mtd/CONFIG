# Configuration for mtd network setup: networking configuration is read from
# u-boot configuration in flash.

include $(commondir)/COMMON

option: network-resolv
	$(startup) $(optiondir)/network.mtd

# vim: set filetype=make:
