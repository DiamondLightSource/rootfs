# Some common makefile definitions and targets to share among options.

RESOLV_CONF ?= $(commondir)/resolv.conf

network-resolv:
ifneq ($(RESOLV_CONF),none)
	$(install) -f $(RESOLV_CONF) /etc/resolv.conf
endif

.PHONY: network-resolv
