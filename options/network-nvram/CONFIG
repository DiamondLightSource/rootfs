# Configuration for nvram network setup: networking configuration is read from
# MOTLoad configuration in nvram.

include $(commondir)/COMMON

option: network-resolv
	$(startup) $(optiondir)/network.nvram

# vim: set filetype=make:
