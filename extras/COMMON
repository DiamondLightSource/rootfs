include $(TOP)/COMMON
include $(scripts)COMPONENT_COMMON

OBJECT_ROOT = $(BUILD_TARGET)/extras

export CROSS_COMPILE = $(COMPILER_PREFIX)-
BUILD_TYPE = $(shell $(TOOLKIT_BIN)/config.guess)

default: all
