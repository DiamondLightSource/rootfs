Only in Python-2.6.patched/: autom4te.cache
diff -ur Python-2.6/configure.in Python-2.6.patched/configure.in
--- Python-2.6/configure.in	2008-09-07 20:18:16.000000000 +0100
+++ Python-2.6.patched/configure.in	2008-11-08 19:14:59.000000000 +0000
@@ -2559,7 +2559,7 @@
 # On Tru64, chflags seems to be present, but calling it will
 # exit Python
 AC_MSG_CHECKING(for chflags)
-AC_TRY_RUN([
+AC_TRY_LINK(,[
 #include <sys/stat.h>
 #include <unistd.h>
 int main(int argc, char*argv[])
@@ -2574,7 +2574,7 @@
 )
 
 AC_MSG_CHECKING(for lchflags)
-AC_TRY_RUN([
+AC_TRY_LINK(,[
 #include <sys/stat.h>
 #include <unistd.h>
 int main(int argc, char*argv[])
@@ -3686,7 +3686,8 @@
 }],
 [AC_MSG_RESULT(yes)
  AC_DEFINE(PY_FORMAT_SIZE_T, "z", [Define to printf format modifier for Py_ssize_t])],
- AC_MSG_RESULT(no))
+ AC_MSG_RESULT(no),
+ AC_MSG_RESULT(do not know))
 
 AC_CHECK_TYPE(socklen_t,,
   AC_DEFINE(socklen_t,int,
